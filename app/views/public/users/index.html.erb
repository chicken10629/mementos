<!-- ユーザー一覧の表示
カレントユーザーではない場合、フォローボタンを表示
-->
<% @users.each do |user| %>
  <%= image_tag user.profile_image %>
  <%= user.name %>
  <%= user.introduction %>
    <% if user.id == current_user.id %>
      <%= link_to "フォローする"
    <% end %>

<% end %>
