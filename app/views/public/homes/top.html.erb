<div class="container">

    <p class="text-secondary fw-light">あなたの思い出の品の記録を残し、共有しよう</p>
    <h1>mementos</h1>

  <div class="row my-2">
    <div class="col ml-auto">
      <h3 class="py-4">New Posts</h3>
<!--        最近の投稿を以下に表示
        投稿日の最新順に並べ替えて5件表示
        必要な情報はpostsモデルの画像、タイトル、ユーザーidに紐づいたユーザーネーム
-->  <% if @posts.present? %>
       <% @posts.each do |post| %>
        <div class="mb-5">
          <% if post.image.attached? %>
            <%= image_tag post.image.variant(resize: "200x200") %>
          <% else %>
            <%= image_tag 'no_image.jpg' %>
          <% end %>
          <p>
            <%= post.title %>
          </p>
          <p>
            <%= post.body %>
          </p>
          <p>
            <%= post.created_at.strftime('%Y/%m/%d %H:%M') %>
          </p>
        </div>
       <% end %>
     <% end %>
    </div>
  </div>
  <div class="row mt-3">
    <div class="col ml-auto">
      <h2>mementosとは？</h2>
      <p>
        
      <p>
    </div>
  </div>
</div>