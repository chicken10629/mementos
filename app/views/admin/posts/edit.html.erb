
<div class="container">
  <div class="post">
    <p>投稿の編集</p>
    <% if @post.errors.any? %>
      <div class="error_messages">
        <ul>
          <% @post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <%= form_with model: @post, url: admin_post_path(@post.id), method: :patch do |f| %>

      <p>
        <%= f.label :title, "タイトル" %>
        <%= f.text_field :title, autofocus: true, class:"w-25" %>
      </p>

      <p>
        <%= f.label :body, "エピソード" %>
        <%= f.text_area :body, class:"w-25" %>
      </p>
      
      <p>
        <%= f.label :image, "画像を選択してください" %>
        <%= f.file_field :image, accept: "image/*" %>
      </p>

      <%= f.submit "変更を保存する" %>
    <% end %>
  </div>
</div>